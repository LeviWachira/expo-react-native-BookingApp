class Room {
    constructor(
        id,
        categoryIds,
        title,
        imageUri,
        timeTitle,
        timeSteps
    ) {
        this.id = id;
        this.categoryIds = categoryIds;
        this.title = title;
        this.title
        this.imageUri = imageUri;
        this.timeTitle = timeTitle;
        this.timeSteps = timeSteps;
    }
}

export default Room;